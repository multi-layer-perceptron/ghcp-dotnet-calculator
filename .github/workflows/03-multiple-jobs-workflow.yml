# Workflow 03: Multiple jobs
# Demonstrates separate jobs that run independently unless needs dependencies are added.
# Docs: https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/use-jobs
# Docs: https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#jobs

name: 03-multiple-jobs-workflow
on:
  push:
  workflow_dispatch:

jobs:
  # Jobs run in parallel by default. Add needs when a later job must wait.
  list-contents:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "🎉 The job was triggered by event: ${{ github.event_name }}"
          echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ."

      - uses: actions/checkout@v4

      - name: list-contents-with-echo
        run: |
          echo "The repository ${{ github.repository }} contains the following files:"
          tree

      # Add a step to list the directory contents of the repository with a powershell script at scripts/Get-DirectoryContents.ps1
      - name: list-contents-with-powershell-script
        shell: pwsh
        run: ./scripts/Get-DirectoryContents.ps1

      # Add a step to list the directory contents of the repository with a powershell script at scripts/Get-DirectoryContents.ps1
      - name: list-contents-with-powershell-inline
        shell: pwsh
        run: Get-ChildItem -Path . -Recurse

      # List the directory contents of the repository using a python script at scripts/getDirectoryContents.py
      - name: list-contents-with-python
        run: python3 scripts/getDirectoryContents.py

  retrieve-values:
    runs-on: ubuntu-latest
    steps:
      - name: get-current-branch
        run: echo "🔎 The current branch is ${{ github.ref }}"
